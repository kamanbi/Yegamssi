# 예감씨 v1.1.46+80 출시 준비

**버전:** 1.1.46+80
**플랫폼:** Android (Google Play Store)

---

## 📦 빌드 파일

| 파일명 | 크기 | SHA256 | 상태 |
|---|---|---|---|
| app-release.apk | 31.5MB | `898733428f637da60ba999a340b545bed0fa1a531728368df592c44f2f4aec93` | ✅ 1.1.46+80 최신 |
| app-release.aab | 52.2MB | `24d1a535a9aba64c459aad43c6c8a40f648ef6d369d308b7f1ad30099aa0fc9f` | ⚠️ 1.1.44+78 — **재빌드 필요** |

> AAB는 이번 버전(위젯 vi 라벨 수정 포함) 이전에 빌드된 파일입니다. Play Console 업로드 전 `tool/build_release.ps1 -Target appbundle -BuildName 1.1.46 -BuildNumber 80` 로 재빌드하세요.

---

## ✨ 주요 변경사항

### 1. 신규 기능: 월간 예감씨
- 홈 하단 탭 재배치: 홈 · 날씨(야외활동 점수 병합) · 운세 · **월간예감(신규)** · 설정
- 카테고리별(연애/일/돈/관계/건강/결정/이동) 이번 달 좋은 날·조심할 날 표시
- 명리학 기반 로컬 규칙(도화·역마·합·충·재성 신살 + 오행 매칭), API/AI 미연동, 완전 deterministic
- 월 1회 생성 후 단일 키 캐시 재사용, 유효성 검사 실패 시 기존 캐시 유지
- 온보딩에 성별 선택 추가 (seed/캐시 키 구성 요소, 기존 사용자 마이그레이션 안전 처리)

### 2. 타로 카드 자산 표준화
- 메이저 아르카나 22장 + 카드 뒷면 전수 표준 양식(1024x1536) 통일
- QA 자동화 스크립트 구축(`.claude/skills/tarot-card-pipeline`), 크기 불일치 카드는 재생성 대신 리사이즈 우선 적용
- WebP 전환으로 자산 용량 62.6MB → 7.6MB (88% 절감)

### 3. 다국어 품질 정비 (11개 언어)
- **베트남어(vi)**: 326개 키 중 282개(87%)가 영어로 방치되어 있던 것을 전면 재번역
- 8개 언어(de/es/fr/hi/ja/pt/ro/zh)의 `settingsSupport*` 8개 키 미번역 보완
- 중국어 날짜 포맷 현지화 (`{year}-{month}-{day}` → `{year}年{month}月{day}日`)
- 베트남어 운세 콘텐츠(`fortune_vi`, 5976행) Supabase 업로드 및 품질 재검증 완료 (오역 2건 → 0건)

### 4. 버그 수정
- **운세 언어 전환 버그**: 앱 언어를 변경해도 운세(`dailyFortuneProvider`)가 Riverpod 상태를 구독하지 않아 이전 언어 결과가 남아있던 문제 수정 (`fortune_provider.dart`)
- **위젯 언어 표시 버그**: 네이티브 위젯(`YegamssiWidget.kt`)의 언어별 라벨 분기에 베트남어(`vi`) 케이스가 누락되어 항상 한국어로 표시되던 문제 수정
- **홈 대시보드 캐시 깜빡임**: 날씨/운세 카드가 캐시된 데이터가 있음에도 백그라운드 재조회 시마다 "로딩중"으로 되돌아가던 문제 수정 (`skipLoadingOnReload` 적용)
- **Supabase 키 빌드 누락**: plain `flutter build` 시 Dart 쪽 Supabase anon key가 `--dart-define`으로 주입되지 않아 앱 화면 API 호출이 실패하던 문제 — 전용 빌드 스크립트(`tool/build_release.ps1`)로 해결

### 5. 보안 점검
- Supabase 키 하드코딩/APK 내 평문 노출 여부 재점검 — 위험 없음 확인 (anon key만 사용, service_role 미노출, RLS 전제)

---

## 🔧 기술 스택

| 구분 | 내용 |
|---|---|
| **Flutter** | 3.44.2 / Dart 3.12.2 |
| **Android SDK** | 최소 24 (Flutter 기본값 상승으로 구형 기기 신규 지원 중단) |
| **핵심 라이브러리** | Riverpod 2.6.1, go_router 14.6.2, Supabase |
| **빌드 방식** | `tool/build_release.ps1` (`.env` → `--dart-define-from-file`) |

---

## 📋 출시 체크리스트

### Google Play Console 업로드
- [ ] AAB 재빌드 (현재 파일은 1.1.44+78, 버전 불일치)
- [ ] app-release.aab (재빌드본) 업로드
- [ ] 릴리즈 노트 초안 (한국어 예시)
  ```
  - 월간 예감씨 추가: 이번 달 카테고리별 좋은 날/조심할 날 확인
  - 다국어 번역 품질 개선 (베트남어 등)
  - 위젯/운세 언어 표시 버그 수정
  - 안정성 개선
  ```
- [ ] 버전: 1.1.46+80 확인

### 테스트 (내부 테스터 트랙)
- [ ] 베트남어/일본어 언어 전환 시 운세·위젯 텍스트 정상 확인
- [ ] 월간예감 탭 진입 및 카테고리 카드 표시 확인
- [ ] 온보딩 성별 선택 UI 확인 (신규 설치 기준)
- [ ] 기존 사용자 업데이트 시나리오 확인 (성별 필드 없는 기존 프로필 마이그레이션)

### 출시 (Production)
- [ ] Internal Testing 검증 완료 후 Production 승격
- [ ] Rollout 단계적 적용 권장

---

## 🗂️ 빌드 경로

```
build/app/outputs/
├── flutter-apk/
│   └── app-release.apk          ← 테스트/직접 설치용 (1.1.46+80 최신)
└── bundle/release/
    └── app-release.aab          ← Google Play Store 배포용 (재빌드 필요)
```

---

## 📝 참고사항

### 알려진 제한사항
- 날씨 화면에서 다른 지역 선택 시에도 야외활동 점수는 항상 현재 위치 기준으로 표시됨 (지역 연동은 후속 과제)
- 다국어 번역은 자동화 QA 통과 수준이며, 원어민 전문 검수는 별도 권장 (특히 베트남어 신규 번역분)
- Android 5~6(API 21~23) 기기는 Flutter SDK 최소 버전 상승으로 신규 설치·업데이트 대상에서 제외됨

### 다국어 상태
- ✅ 한국어(ko): 완성
- ✅ 일본어(ja), 중국어(zh), 힌디어(hi): QA 통과, 실제 결함 없음
- ✅ 베트남어(vi): 이번 버전에서 전면 재번역 완료
- ✅ 독일어/스페인어/프랑스어/포르투갈어/루마니아어: QA 통과, 잔여 항목은 언어 간 동일 철자 단어(고유명사/외래어)로 정상

---
---

**문서 작성일:** 2026-07-06
**준비 상태:** 🟡 AAB 재빌드 후 Google Play Console 배포 준비 완료
