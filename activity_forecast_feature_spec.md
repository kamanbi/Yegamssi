# 활동예감 추가 기능 최종 명세

## 1. 목표
- 기능명은 `활동예감`으로 한다.
- 사용자가 위치와 활동 시간을 입력하면 날씨·환경·해양 데이터를 결합해 활동 적합도와 안전 판단을 제공한다.
- 정보 나열이 아니라 `지금 해도 되는지`, `언제가 더 나은지`, `무엇이 위험한지`를 설명하는 판단 기능으로 제공한다.
- 1차 지원 활동은 바다낚시, 걷기/달리기, 등산, 빨래, 세차다.

## 1.1 현재 시스템 전제
- 날씨 공급자 응답은 Supabase `weather-proxy`의 L1 인메모리 캐시와 L2 Postgres `weather_proxy_cache`를 거친다.
- 기존 프록시의 공급자 허용목록, 쿼리 정규화, 경로별 TTL, single-flight, 오류 시 upstream 폴백을 그대로 사용한다.
- KMA 단기·초단기 예보는 발표시각과 격자 좌표가 캐시 키에 포함되며 기존 TTL은 발표 주기에 맞춰 고정되어 있다.
- KMA 예보 항목이 최소 개수에 미달하면 성공 응답이어도 저장하지 않는 `E12` 검증을 유지한다.
- L2 조회 제한시간 `700ms`와 기존 KMA·대기질·OpenWeather TTL은 활동예감 작업에서 변경하지 않는다.
- 활동예감은 기존 날씨 캐시 구조를 교체하거나 별도 날씨 프록시를 만들지 않고, 검증된 캐시 응답을 입력 데이터로 소비한다.

## 1.2 구현 기준 상태
- 기준일은 2026-08-12이며 계산 버전은 `activity-v5`이다.
- 현행 국가어항 115건, 목적지 ID 마이그레이션, 활동 5종 입력 반영, 활동 전용 캐시와 이력 20개 제한이 구현됐다.
- 바다낚시지수, 기상특보 현황, 시도 산불위험 API는 실제 운영 응답 계약을 검증해 활성화했다.
- 조석·조류·ROMS·부이·실측파랑과 등산로 API는 승인·호출 예산·좌표 매핑이 모두 검증되기 전까지 점수에 사용하지 않는다.
- 정적분석 0건과 전체 자동 테스트 149건을 통과했다.
- `1.1.53+87` 릴리즈 APK를 공식 빌드 스크립트로 생성해 Samsung SM-S908N에 설치하고 `targetSdk=36`을 확인했다.
- 실기기에서 빨래 D+7 `건조 좋음`, 세차 D+7·48시간 `유지 좋음`, 등산 D+7 `안전 미확정`, 바다낚시 공식 일 자료의 위험 중단, D+7 중기 해역 판단을 검증했다.

## 2. 제공 국가와 진입 정책
- 기기의 현재 물리적 위치가 대한민국으로 확인된 경우에만 노출한다.
- 앱 언어, 시간대, SIM 국가, 선택한 날씨 도시로 노출 여부를 결정하지 않는다.
- 전용 상태는 `checking`, `eligible`, `ineligible`로 분리한다.
- GPS 국가 확인 실패, 권한 거부, 조회 시간 초과, 해외 위치, 오래된 국가 캐시는 모두 `ineligible`로 처리한다.
- 확인 중이거나 비대상인 경우 기존 `월간예감` 하단 메뉴를 유지한다.
- 대한민국으로 확인되면 하단 `월간예감`을 `활동예감`으로 교체하고 월간예감은 운세 페이지 내부에서 제공한다.
- 한국 운세 화면에는 `오늘의 운세 / 월간예감` 전환 진입점을 제공하고 `/monthly` 직접 진입에서도 하단 운세 탭을 선택 상태로 유지한다.
- 해외에서는 기존 월간예감 하단 탭을 유지한다.
- 비대상 상태에서는 탭, 홈 진입 버튼, 딥링크, 저장 결과 상세를 모두 차단한다.
- 저장 이력은 해외에서 삭제하지 않고 숨겼다가 대한민국에서 다시 노출한다.
- 활동 장소 검색도 대한민국 영역으로 제한한다.

## 3. 화면 구조
- 상단에 바다낚시·걷기/달리기·등산·빨래·세차 전용 생성 이미지 아이콘 버튼 5개를 표시한다.
- 활동 이미지에는 문구를 포함하지 않고 앱 텍스트와 접근성 레이블을 별도로 제공한다.
- 하단에는 고정 항목과 최근 판단 이력을 최신순으로 표시한다.
- 바다낚시와 등산은 현재 위치 즉시 판단이 아니라 저장 가능한 `활동 계획`을 생성하는 흐름으로 제공한다.
- 판단 상태는 `상세 판단`, `계획 판단`, `예보 대기`로 구분한다.
- 시간별 예보가 일정 전체를 포함하면 상세 판단, 일별·오전·오후 예보만 있으면 계획 판단, 날짜 예보가 없으면 예보 대기로 처리한다.
- 이력 항목에는 활동, 장소, 예정일시, 점수, 안전 상태, 데이터 기준시각을 표시한다.
- 이력은 최대 20개이며 오래된 미고정 항목부터 자동 정리한다.
- 모든 항목이 고정 상태이면 자동 삭제하지 않고 사용자가 먼저 항목을 삭제하도록 안내한다.
- 최근 판단 카드는 활동 이미지 없이 첫 줄에 `활동, 지역, 일시`, 둘째 줄에 `판단 상태 · 점수 · 만료 여부`를 표시한다.
- 숫자 점수는 `37점`, 계획 범위는 `46~70점`으로 표시하고 문자 상태에는 `점`을 붙이지 않는다.
- 카드 우측에는 16px 고정/해제·삭제 아이콘을 각각 24px 버튼 영역으로 가로 배치한다. 상세의 `삭제`와 화면 상단의 `전체 삭제`도 유지한다.
- 긴 문구는 말줄임표 대신 카드 폭 안에서만 축소하고 카드 높이는 2줄 수준을 유지한다.

## 4. 다이얼로그 흐름
- 작은 AlertDialog가 아닌 시스템 상태바 안전영역 아래를 최대한 채우는 닫기 가능한 전체 높이 모달 시트를 사용한다.
- 별도 드래그 핸들은 두지 않고 96px 높이의 헤더에 활동 이미지, 활동명과 닫기 버튼을 배치한다.
- 최초 진입은 입력 상태, 제출 후에는 같은 모달 안에서 로딩 상태를 거쳐 결과 상태로 전환한다.
- 결과 상태에서도 모달을 닫을 수 있고, 다시 열면 마지막 저장 결과를 즉시 표시한다.
- 상세·계획 판단 모두 긍정 요인, 위험 요인, 미확인 자료, 행동 권고와 최대 3개의 대안 날짜·시간을 상세히 표시한다.
- 계획 판단은 점수 범위와 신뢰도로 표시한다. 바다낚시·등산의 필수 안전자료가 부족하면 `안전 미확정`을 함께 표시한다.
- 빨래·세차는 중기 일별예보가 근거 구간을 덮으면 최종 생활 적합도를 확정하며, 미래 습도·바람·미세먼지 누락만으로 판단을 제한하지 않는다.
- 결과 하단에 `조건 변경`, `다시 계산`, `저장`, `삭제` 명령을 동일 크기 2×2 터치 영역으로 제공한다.
- 버튼은 `조건 변경 | 다시 계산 / 저장 | 삭제` 순서로 배치한다.
- 네 명령은 같은 스타일의 생성 이미지 4종을 사용하고 배경 박스·외곽선을 두지 않는다.
- 이미지 크기, 문구 색상·크기·줄 수·가운데 정렬과 터치 영역을 동일하게 하며 시스템 글자 크기 확대와 소형 화면에서도 잘리거나 겹치지 않아야 한다.
- 삭제를 포함한 네 명령의 시각 규격은 동일하게 유지하고 삭제 확인 절차로 파괴적 동작을 구분한다.
- 삭제 명령은 오류색 외곽선으로 구분하고 나머지 명령은 기능 우선순위에 맞는 tonal·outline·text 계층을 사용한다.
- 조건 변경은 기존 입력값을 유지한 채 입력 화면으로 돌아간다.
- API 실패 시 기존 성공 결과를 삭제하지 않고 실패 원인과 마지막 성공 시각을 표시한다.
- 바다낚시와 등산은 목적지와 일정을 먼저 저장한 뒤 예보 가능 범위이면 즉시 판단한다.
- 일정이 예보 가능 범위 밖이면 점수를 추정하지 않고 `예보 대기`와 판단 가능 예정시점을 표시한다.
- 결과 스크롤 영역과 하단 명령·광고 영역을 분리한다.
- 결과 요약 영역은 모서리 반경 0의 사각형으로 표시한다.
- 점수와 가능 여부는 줄바꿈 없이 같은 행에 배치하고 전체를 중앙 정렬한다.
- 결과 요약 영역 패딩은 12px를 기준으로 하고 요약과 판단 근거 행은 compact 밀도로 구성한다.
- 하단은 50px 배너 광고 슬롯, 요소 간 간격과 기기 시스템 내비게이션 `SafeArea`를 반영하며 광고 로딩 전후에도 결과와 버튼의 위치가 겹치지 않아야 한다.
- 프리미엄 사용자는 광고 슬롯을 제거하되 시스템 `SafeArea`와 최소 조작 여백은 유지한다.
- 홈과 모달이 동시에 별도 배너 광고를 로딩하거나 노출하지 않으며 한 시점에 한 광고 슬롯만 소유한다.

## 5. 공통 입력
- 활동 종류
- 활동 목적지의 대한민국 내 장소명과 좌표
- 활동 날짜
- 시작 시간은 현재 값을 초기 위치로 하는 `오전/오후 · 시 · 분` 휠 피커로 선택한다.
- 시간 선택을 취소하면 달력에서 먼저 고른 날짜를 포함해 기존 시작 일시를 유지한다.
- 활동 시간
- 일정은 예보 범위 밖도 저장할 수 있지만 점수는 요청 시간 전체가 예보 범위에 들어온 뒤에만 계산한다.
- 장소, 시간 범위 또는 필수 활동 옵션이 유효하지 않으면 API를 호출하지 않고 즉시 오류를 표시한다.
- 바다낚시와 등산은 현재 위치를 목적지 기본값으로 사용하지 않는다.
- 시작일시와 활동시간으로 종료일시를 계산하며 모든 날씨·안전자료는 이 구간과 겹치는 값만 사용한다.

## 6. 활동별 입력
### 바다낚시
- 사용자가 계획하는 `출조 목적지`와 국립해양조사원 바다낚시지수의 `공식 기준점`을 분리한다.
- 출조지는 현행 국가어항 115곳을 번들 카탈로그에서 검색한다. 직접 검색한 대한민국 연안은 공식 어항 또는 공식 낚시지점 25km 이내인 경우에만 대표 좌표로 저장한다.
- 공식 바다낚시지수 40개 지점과 유형별 지원 어종은 검증된 번들 스냅샷으로 표시하고 정확히 일치하는 경우에만 해당 지수를 사용한다.
- 공식 기준점 밖의 목적지를 가장 가까운 공식 지점과 동일한 장소로 간주하거나 그 지수를 그대로 적용하지 않는다.
- 공식 지수가 없는 목적지는 목적지 좌표·일정에 맞는 날씨와 조석·조류·ROMS·관측·파랑 자료로 판단하고 `공식 지수 미지원`과 커버리지 수준을 표시한다.
- 필수 해양자료가 확보되지 않은 목적지는 날씨만으로 확정 점수를 만들지 않고 `자료 부족`으로 안내한다.
- 공식 해양자료가 있어도 바다낚시지수 값을 해석할 수 없으면 점수 없이 `안전 미확정`으로 표시한다.
- 낚시 유형: 갯바위 또는 선상
- 대상 어종: 공식 기준점이면 API 지원 어종, 그 외 목적지는 별도 목적지 카탈로그가 검증한 어종만 제공한다.
- 출조 날짜, 시작 시간과 종료 시간
- 대한민국 해역 검증을 통과하지 못한 임의 좌표에는 바다낚시 판단을 제공하지 않는다.
- 민물낚시는 1차 범위에서 제외한다.

### 걷기/달리기
- 활동 유형: 걷기 또는 달리기
- 강도: 가볍게, 보통, 강하게

### 등산
- 산림청 산 정보에서 산명·소재지를 검색하고, 등산로 API 승인 전에는 해당 공식 소재지를 지오코딩한 좌표를 판단 좌표로 사용한다.
- 사용자 수준: 초보, 보통, 숙련
- 산행 날짜, 시작 시간과 예상 종료 시간
- 등산로 난이도와 예상 소요시간은 산림 API 값이 있으면 자동 적용한다.

### 빨래
- 건조 방식: 실외 또는 베란다
- 빨래 상태: 일반 또는 두꺼운 빨래

### 세차
- 세차 방식: 일반 또는 정밀
- 세차 후 야외 주차 예정시간: 기본 24시간, 선택 48시간
- 세차 결과에는 활동 종료와 별도로 시작 후 24/48시간의 `유지 판단 범위`를 표시한다.

## 7. 공통 결과
- 종합점수: 0~100
- 판단등급: 매우 좋음, 좋음, 보통, 나쁨, 매우 나쁨
- 바다낚시·등산 안전상태: 가능, 주의, 중단 권고, 안전 미확정
- 빨래 건조 전망과 세차 유지 전망: 좋음, 보통, 비추천
- 한 줄 결론과 사용자 행동 문장
- 요청 시간의 상세 판단과 같은 날짜 안의 추천 시간대
- 점수에 영향을 준 긍정·부정 요인과 각 기여도
- 데이터 기준시각, 예보 유효시간, 출처, 관측소 거리, 커버리지 수준
- 캐시 결과인 경우 캐시 여부와 예보 변경 또는 재계산 필요 상태
- 예보 범위 밖의 계획은 점수 대신 `예보 대기`, 예정 장소·일정과 판단 가능 예정시점을 표시한다.
- 일별 예보가 있는 미래 계획은 계획 적합도 범위, 확인된 근거, 미확인 안전자료와 대안 일정을 표시한다.
- 바다낚시·등산 안전상태는 점수와 독립적으로 계산하며 `중단 권고`가 종합점수보다 우선한다.
- 빨래·세차는 인명 안전상태를 표시하지 않고 중기예보 기반 생활 적합도를 확정한다.
- 점수는 활동 적합도이며 조과, 운동 성과, 건조 완료 등을 보장하지 않는다.

## 8. 활동별 판단 요소
- 바다낚시: 공식 바다낚시지수, 파고, 풍속, 강수·낙뢰, 물때, 조위, 유향·유속, 수온과 대상 어종 적합도
- 걷기/달리기: 강수, 체감온도, 대기질, 풍속, 자외선, 노면 위험
- 등산: 강수·낙뢰, 풍속, 체감온도, 대기질·자외선, 산불위험, 등산로 난이도와 사용자 수준
- 빨래: 강수 가능성, 습도, 풍속, 일사·운량, 미세먼지
- 세차: 세차 시점과 이후 24/48시간 강수, 미세먼지, 바람, 기온, 오염 재발 가능성
- 빨래·세차 중기 판단은 강수·날씨·확인 가능한 기온으로 확정하고, 시간대 습도·바람·미세먼지는 상세예보가 있을 때만 보조 요인으로 사용한다.
- 세차 48시간에 기간 자체의 고정 감점을 적용하지 않고 실제 유지 구간의 최대 강수확률과 강수 상태를 계산한다.
- 활동별 가중치와 안전 임계값은 화면 코드에 직접 작성하지 않고 버전이 있는 정책 상수로 관리한다.

## 9. 안전 우선 규칙
- 기상특보, 낙뢰, 위험 파고·풍속·유속, 매우 높은 산불위험 등은 점수 계산 전에 안전 중단 조건으로 판정한다.
- 안전 중단 조건이 성립하면 높은 적합도 점수가 계산되더라도 `중단 권고`를 표시한다.
- 바다낚시·등산 데이터가 오래됐거나 필수 안전 데이터가 누락되면 좋은 점수를 추정하지 않고 `판단 제한` 또는 `안전 미확정`을 표시한다.
- 빨래·세차는 근거 기간의 중기 강수·날씨가 있으면 판단을 확정하고 부가 자료 누락은 신뢰도와 미확인 자료에만 반영한다.
- 임계값은 출처와 근거를 문서화하고 계산 버전에 포함한다.
- 기상특보는 `getPwnStatus`의 현행 발효내용과 기준시각을 사용하고 10분 캐시한다.
- 출발 6시간 이내 바다낚시에만 목적지 좌표 기반 해역을 대조한다. 갯바위는 먼바다 전용 특보를 제외하고 선상은 같은 권역의 먼바다 특보를 포함한다.
- 강풍·풍랑·태풍·폭풍해일·지진해일 특보가 목적지 권역과 일치하면 점수를 만들기 전에 중단 권고한다.

## 10. 바다낚시 데이터 정책
- 신규 바다누리 국가중점 `바다낚시지수`를 공식 기준점으로 사용한다.
- 공식 지수는 갯바위/선상, 어종, 물때, 파고, 수온, 유속, 풍속과 5단계 결과를 제공하며 하루 2회 갱신된다.
- 공식 지수를 예감씨 종합점수 전체로 복사하지 않고 하나의 기준 요인으로 사용한다.
- 사용자가 지정한 시간은 KMA 시간별 날씨와 KHOA 조석·조류 시계열을 결합해 보정한다.
- 공식 지점은 `공식 지점 판단`, 좌표 기반 보완은 `좌표 기반 판단`으로 구분해 표시한다.
- 임의 좌표는 거리·최신성 기준을 통과한 인접 관측소 또는 좌표형 예측만 사용한다.
- 기준을 통과하지 못하면 육상 날씨로 해양 상태를 추정하지 않고 `해양자료 지원 범위 밖`을 반환한다.
- D+0~2는 요청 시간과 겹치는 오전·오후 자료만 사용하며 다른 시간대 자료로 대체하지 않는다.
- D+3~6의 공식 `일` 자료는 해당 날짜 전체의 계획 근거로 사용하되 특정 시간대 해양 상태처럼 표시하지 않는다.
- 어종 미선택 시 지원 어종 전체의 가장 낮은 공식지수를 사용한다.
- 필수 파고·풍속 누락, 알 수 없는 지수, 목적지에서 30km를 넘는 관측소는 판단 제한으로 처리한다.
- D+7~10은 기상청 중기 해상예보의 해역별 날씨·파고와 중기 육상·기온예보로 출조 환경만 판단한다.
- D+7~10에는 공식 어종별 지수가 없으므로 조황점수를 만들지 않고 시간대 해상풍·물때·출항 안전을 미확정으로 표시한다.
- 중기 해상예보는 최신 06/18시 발표를 우선하며 아직 생산되지 않았으면 직전 발표로 폴백하고 12시간 캐시한다.

## 11. 공공 API 활용신청 현황
- 기준일은 2026-08-11이다.
- 활동예감용 활용신청은 총 14건이며 낚시 관련 10건, 등산 관련 4건이다.
- 현재 상태는 승인 13건, 신청 접수 1건이다.
- 공공데이터포털 계정의 전체 신청 건수에는 활동예감과 무관한 기존 API가 포함될 수 있으므로 본 절의 14건과 구분한다.
- 승인 상태만으로 연동 완료로 판단하지 않는다. 구현 전 실제 서비스 URL, 응답 포맷, 필수 필드, 일일 트래픽과 운영계정 전환 조건을 각각 스모크 테스트한다.

### 11.1 낚시 관련 10건
| 번호 | 공공데이터포털 API | 상태 | 활동예감 사용 목적 |
|---:|---|---|---|
| 1 | 국립해양조사원 `바다낚시지수` | 승인 | 공식 낚시지점, 갯바위·선상, 어종별 5단계 지수를 기준 요인으로 사용 |
| 2 | 국립해양조사원 `조석예보(고, 저조)` | 승인 | 만조·간조 시각과 조위 판단 |
| 3 | 국립해양조사원 `조석예보(시계열)` | 승인 | 사용자 활동 시간대의 조위 변화 판단 |
| 4 | 국립해양조사원 `조류예보(시계열)` | 승인 | 시간대별 유향·유속 판단 |
| 5 | 국립해양조사원 `조류예보 최강창낙조 및 전류` | 승인 | 최강 창·낙조와 전류 시각에 대한 안전 판단 |
| 6 | 국립해양조사원 `ROMS 수치예측모델 조회` | 승인 | 관측 공백 구간의 해류·수온 등 예측자료 보완 |
| 7 | 국립해양조사원 `해양관측부이 최신 관측데이터` | 승인 | 최근 해양 관측값과 예측값 교차검증 |
| 8 | 국립해양조사원 `국가해양관측망 실측 파랑` | 승인 | 실측 파고와 출조 안전 임계값 판정 |
| 9 | 기상청 `전국 해수욕장 날씨 조회서비스` | 승인 | 해안 지점의 기온·풍속·강수 등 육상 기상 보완 |
| 10 | 기상청 `기상특보 조회서비스` | 승인 | 강풍·풍랑·호우 등 안전 중단 조건 판정 |

### 11.2 등산 관련 4건
| 번호 | 공공데이터포털 API | 상태 | 활동예감 사용 목적 |
|---:|---|---|---|
| 1 | 기상청 `생활기상지수 조회서비스(4.0)` | 승인 | 자외선지수와 대기정체지수 반영. 신청된 상세기능 범위를 넘어 다른 생활지수를 가정하지 않음 |
| 2 | 산림청 국립산림과학원 `산불위험예보정보` | 승인 | 산불위험 등급을 등산 안전 중단·주의 조건에 반영 |
| 3 | 산림청 `산 정보 조회_GW` | 승인 | 산명, 위치, 높이, 관리주체와 주변 정보 조회 |
| 4 | 산림청 `산림공간정보_등산로정보_GW` | 신청 접수 | 등산로 위치와 상세 정보를 산·코스 선택 및 난이도 판단에 사용. 승인 전 호출 금지 |

### 11.3 API 적용 규칙
- 바다낚시지수는 종합점수의 단일 입력 요인이며 예감씨 점수로 그대로 복사하지 않는다.
- 조석·조류·파랑·부이·ROMS는 관측소 거리, 기준시각, 예측 유효시간과 커버리지를 검증한 뒤 사용한다.
- 기상특보와 산불위험은 가점·감점 요소가 아니라 안전 우선 규칙의 중단 또는 주의 조건으로 처리한다.
- 생활기상지수 조회서비스(4.0)는 현재 신청 화면에서 제공된 자외선지수와 대기정체지수만 연동 대상으로 한다.
- 등산로정보_GW가 승인되기 전에는 산 정보와 기존 날씨 데이터만으로 제한 결과를 제공하고, 등산로 난이도·소요시간을 임의 추정하지 않는다.
- 동일 데이터의 구형 API와 GW API가 함께 검색되면 신규 구현은 승인받은 GW 항목을 우선하며 실제 응답 계약 확인 후 하나만 사용한다.

### 11.4 인증정보
- 공공데이터포털 API는 서버의 `PUBLIC_DATA_API_KEY`를 사용한다.
- KHOA에서 별도 발급되는 키가 있으면 `KHOA_API_KEY`로 분리한다.
- API 키는 앱 바이너리와 로컬 캐시에 저장하지 않고 Supabase Edge Function Secrets에서만 관리한다.
- 폐기된 구 바다누리 엔드포인트는 사용하지 않는다.

## 12. 저장과 캐시
- `SavedActivityPlan`, 활동별 타입 옵션, `ActivityJudgment`, `JudgmentFactor`, `SafetyLevel`을 명시적인 모델로 정의한다.
- 바다낚시·등산 계획은 결과가 없어도 목적지 좌표, 공식 지점/산 식별자, 시작·종료일시와 옵션을 저장할 수 있다.
- 저장 계획이 예보 범위에 들어오면 앱 진입 또는 수동 재판단 시 최신 자료로 계산하며 과거 결과를 현재 결과처럼 표시하지 않는다.
- 캐시 키에는 활동 종류, 좌표, 요청 시간 범위, 활동 옵션, 계산 버전을 포함한다.
- 판단 결과에는 사용한 원본 데이터의 기준시각과 만료시각을 저장한다.
- 여러 출처를 사용하면 가장 먼저 만료되는 필수 출처를 결과 만료 기준으로 삼는다.
- 저장 위치와 활동 이력은 암호화된 로컬 저장소에 보관하고 서버 계정과 자동 동기화하지 않는다.
- 원본 날씨 캐시, 선택 위치 캐시, 운세 캐시, 위젯 저장소와 완전히 분리한다.
- 활동 캐시는 현재 위치 날씨 또는 위젯 내용을 덮어쓸 수 없다.
- 1차 버전에서는 활동 결과를 위젯에 표시하지 않는다.
- 공급자 원본 응답 캐시와 활동 판단 결과 캐시를 구분한다.
- 공급자 원본 응답은 기존 `weather-proxy` L1/L2 캐시에 저장하고, 활동 판단 결과는 앱의 암호화된 로컬 저장소에 저장한다.
- 활동 판단에 사용한 위치·시간별 날씨와 해양 근거자료는 현재 위치·즐겨찾기 날씨 캐시와 분리된 활동 전용 캐시에 저장한다.
- 활동 전용 캐시는 좌표 격자, 계획 시간 구간, 자료 종류, 공급자 발표시각과 계산 버전을 키로 사용한다.
- 동일 공급자 요청은 기존 canonical cache key와 single-flight를 사용하며 활동 종류별로 중복 원본 요청을 만들지 않는다.
- L2 장애 또는 시간 초과 시 기존 동작대로 upstream으로 폴백하되, 활동 결과에는 사용 데이터의 실제 기준시각과 커버리지 상태를 기록한다.
- 신규 API별 TTL은 해당 기관의 생산·갱신 주기를 근거로 별도 상수화하며 기존 날씨 TTL 표를 수정하지 않는다.
- 신규 API도 정상 HTTP 상태만으로 캐싱하지 않고 필수 필드·최소 항목 수·기준시각을 검증한 뒤 저장한다.

### 12.1 호출 예산
- 화면 진입, 저장 결과 열기, 조건 변경과 새 판단 입력 시작은 API를 호출하지 않는다.
- 유효한 활동 근거 캐시가 있으면 판단과 `다시 계산`은 Edge Function과 공급기관 API를 모두 호출하지 않는다.
- `다시 계산`은 캐시 우회 또는 원본 강제 갱신 명령이 아니다.
- 출조지와 공식 지수 기준점 카탈로그는 앱 번들에서 표시하며 목록 열기·검색·이력 복원은 서버나 공급기관 API를 호출하지 않는다.
- 공식 지수 기준점 스냅샷은 공급기관 지점·지원 어종 변경을 검증한 앱 릴리즈에서만 갱신한다.
- 캐시 MISS 시 앱은 활동 자료 묶음 요청을 최대 1회 보내고 서버는 해당 활동에 필수이며 만료된 자료만 조회한다.
- 같은 키의 동시 요청은 앱과 서버 양쪽에서 single-flight로 합친다.
- 현재 위치 날씨는 기존 30분 루프만 유지하며 저장 활동 목적지를 이 루프에 추가하지 않는다.
- 저장된 모든 활동 계획을 30분마다 개별 갱신하지 않는다.
- 저장 결과를 열었을 때 마지막 계산 후 6시간이 지났고 아직 상세 판단이 아니면 해당 한 건만 캐시 우선으로 자동 재판단한다.
- 현재 특보는 출발 6시간 이내의 상세 바다낚시 판단에서만 조회한다.
- 호출량 로그는 기능, 캐시 HIT/MISS, 묶음 요청 수와 실제 upstream 요청 수를 분리 집계하며 목표 초과 시 신규 자료 연동을 중단한다.

### 12.2 기존 날씨 갱신과 위젯 보존 조건
- 현재 위치 날씨의 앱·백그라운드 갱신은 동일한 30분 정책과 동일한 현재 위치 캐시를 사용한다.
- Android의 30분 주기는 정확한 정각 실행이 아니라 최소 반복 간격이며, 실제 실행 시각은 OS 작업 창과 제조사 전원 정책에 따라 지연될 수 있다.
- WorkManager 주기 작업은 `ExistingPeriodicWorkPolicy.keep`으로 유지해 앱 실행마다 작업 세대와 기준 시각을 재설정하지 않는다.
- 백그라운드 작업은 저장된 현재 위치 좌표를 우선 사용하고, 좌표가 없을 때만 현재 위치 공급자를 조회한다.
- 공급자·네트워크 도메인 실패는 기존 성공 캐시를 보존하고 작업을 성공 종료해 10분 재시도 폭주를 만들지 않는다. 다음 정상 30분 주기에서 다시 시도한다.
- 예기치 않은 프로그래밍·플랫폼 오류만 WorkManager 재시도 대상으로 처리하고 오류 유형과 메시지를 로그에 남긴다.
- 배터리 최적화 예외가 없으면 제조사 정책으로 백그라운드 네트워크가 차단될 수 있다. 영구 숨김 없는 반복 안내와 설정 화면을 제공하고 사용자가 시스템 화면에서 직접 허용한다.
- 위젯은 외부 API나 활동 API를 호출하지 않고 현재 위치 날씨·야외점수·운세 공용 스냅샷만 표시한다.
- 활동 목적지와 활동 근거 캐시는 현재 위치 캐시 및 위젯 스냅샷을 갱신하거나 덮어쓸 수 없다.
- 앱 화면 진입은 캐시만 표시하고 날씨가 갱신 기한에 도달하지 않았다면 API를 호출하지 않는다. 수동 날씨 갱신은 즉시 현재 위치 캐시를 갱신하고 위젯을 동기화한다.
- 앱과 위젯은 동일한 온도, 체감온도, 날씨 상태, 낮·밤 아이콘, 야외점수와 운세 방향을 사용한다.

### 12.3 미래 일정 예보 보존 조건
- KMA 시간별 예보는 24개 저장 제한으로 절단하지 않고 완전한 시간 그룹을 공급자 응답 범위까지 보존한다.
- 일반 날씨 화면은 보존된 시간별 자료 중 24개, 일별 자료 중 7개만 표시한다.
- 활동 판단은 보존된 전체 시간별·일별 자료를 사용하며 72시간 같은 고정 예보 범위를 가정하지 않는다.
- 미래 시간별 풍속·강수확률·습도·자외선이 없으면 현재 관측값으로 대체하지 않고 미확인 자료로 기록한다.
- 일별예보는 오전·오후 강수확률을 별도 보존하고 요청 시간대와 겹치는 값만 계획 판단에 사용한다.
- 중기 기온 응답이 누락되면 0도로 저장하지 않고 기온 미확인 상태로 보존한다.
- 세차 24/48시간은 근거 구간에 포함된 모든 날짜의 일별 또는 시간별 예보가 있어야 판단한다.
- 판단 결과 저장 스키마는 버전 4이며 구버전 `forecastPending` 결과는 `pending`으로 복원한다.

## 13. 프록시 보안
- 모든 외부 API 키는 Supabase Edge Function Secrets에만 저장한다.
- 앱은 허용된 정규화 API만 호출하고 공급자 URL과 API 키를 전달받지 않는다.
- 기존 처리 순서인 `provider/path 검증 → provider key 확인 → 캐시 조회 → upstream`을 보존한다.
- 신규 KHOA·공공데이터 경로는 명시적인 공급자 설정, 허용 경로, 인증키 파라미터, TTL이 모두 등록된 경우에만 호출한다.
- 앱/anon 요청의 `nocache=1`은 거부하거나 무시하고, 현재 검증용 캐시 우회 경로 자체는 삭제하지 않는다.
- 강제 원본 갱신은 별도 서버 인증을 통과한 스케줄러 또는 운영 검증 요청만 수행한다.
- 수동 재계산도 서버가 캐시 유효성을 판단하며 클라이언트가 원본 호출을 강제하지 못한다.
- rate limit 검사는 허용 경로와 provider key 검증 직후, 캐시 조회 전에 독립 가드로 적용한다.
- 호출자·IP별 요청 제한과 공급자별 upstream 할당량·동시호출 제한을 분리한다.
- 캐시 HIT는 공급자 upstream 할당량을 소비하지 않으며 MISS/BYPASS 원본 호출만 공급자 할당량에 반영한다.
- 초과 요청은 `429`와 `Retry-After`를 반환하고 동일 요청은 single-flight로 합친다.
- 로그에는 키, 인증 헤더, 원본 좌표, 개인정보를 남기지 않는다.
- 반복 우회 시도, 공급자 오류율, 할당량 소비율에 경보 기준을 둔다.
- L2 캐시 테이블은 service role만 접근하고 anon/authenticated 직접 조회 정책을 만들지 않는다.

## 13.1 기존 프록시 보존 조건
- `weather_proxy_cache` 테이블과 만료 인덱스, RLS, pg_cron 또는 확률 기반 만료 정리 폴백을 유지한다.
- L1 최대 용량, 엔트리 제한, 좌표 스냅, 캐시 키 canonicalization을 활동예감 작업 범위에서 재설계하지 않는다.
- 기존 KMA 경로의 `E12` 최소 항목 검증과 오류 응답 캐시 제외 규칙을 삭제하거나 완화하지 않는다.
- 기존 `nocache`와 일반 MISS의 single-flight 키 분리를 유지한다.
- 캐시 계층 오류가 날씨 또는 활동 요청 전체 실패로 전파되지 않도록 기존 fail-open 동작을 유지한다.
- 프록시 공통부 수정은 보안 가드와 신규 공급자 확장에 한정하고 캐시 판정 순서를 재배치하지 않는다.

## 14. 가독성 설계
- Early Return: 잘못된 장소·시간·옵션, 지원범위 밖, 안전 중단 조건을 계산 전에 종료한다.
- Contextual Naming: `requestedWindow`, `forecastObservedAt`, `coverageLevel`, `safetyOverride`, `scoreContributions`를 사용한다.
- Magic Number Hunter: 점수 가중치, 거리, 최신성, 이력 20개, rate limit을 이름 있는 정책 상수로 관리한다.
- Parameter Object: 위치·시간·옵션을 개별 인자로 넘기지 않고 `ActivityJudgmentRequest`로 전달한다.
- Complexity Check: 현재 예상 가독성 6/10, 구현 후 목표 8/10이며 계산기별 순환복잡도는 10 미만으로 유지한다.

## 15. 남은 구현 순서
1. `등산로정보_GW` 승인 후 공식 코스 좌표·난이도·소요시간 계약을 검증한다.
2. 호출 예산과 관측소 거리 기준을 확정한 뒤 조석·조류·ROMS·부이·실측파랑을 한 공급자씩 활성화한다.
3. KMA 44개 해양 특보구역 폴리곤 또는 공식 공간 매핑이 확보되면 현재 좌표 기반 권역 규칙을 교체한다.
4. 신규 판단 문구의 다국어, 접근성, 분석 이벤트와 출시 회귀 검증을 완료한다.

## 15.1 빌드와 배포
- 릴리즈 APK/AAB는 반드시 `tool/build_release.ps1`로 빌드한다.
- 일반 `flutter build apk/appbundle --release` 직접 실행을 금지한다.
- 빌드 결과에 Supabase URL과 anon key가 컴파일타임 설정으로 주입됐는지 확인한다.
- 실기기에서 활동예감뿐 아니라 날씨, 운세, 위젯 미러링을 함께 회귀 검증한다.

## 16. 완료 기준
- 대한민국에서만 모든 진입 경로가 노출되고 해외·미확인 상태에서는 완전히 차단된다.
- 활동 선택부터 입력, 같은 모달의 결과 표시, 닫기, 복원, 조건 변경, 재계산, 저장, 이력 열기와 삭제가 동작한다.
- 저장 결과는 즉시 표시되고 데이터 변경·만료·판단 제한 상태가 구분된다.
- 모든 결과에 점수 근거, 활동별 상태, 데이터 시각, 출처와 커버리지가 표시된다.
- 활동 캐시가 날씨·운세·위젯 캐시를 변경하지 않는다.
- 앱 호출로 원본 API 캐시를 강제 우회할 수 없고 과도한 호출은 안정적으로 `429` 처리된다.
- 해양 지원범위 밖에서 육상 날씨 기반의 허위 낚시 적합도를 생성하지 않는다.
- 계산기, 직렬화, 암호화 저장, 캐시 마이그레이션, 국가 제한, 라우팅, rate limit, API 계약, 모달 상호작용 테스트가 통과한다.
- 기존 날씨 경로에서 L1/L2 HIT, MISS, 불완전 응답 제외, L2 시간 초과 폴백이 활동예감 추가 전과 동일하게 동작한다.
- 기존 KMA 경로별 TTL, 좌표 정규화, `E12`, L2 `700ms`, single-flight 동작에 회귀가 없다.
- 앱을 닫은 실기기에서 배터리 최적화 예외 적용 후 자연 WorkManager 주기가 날씨·야외점수·운세 캐시를 갱신하고 위젯 동기화 후 `SUCCESS`로 종료한다.
- 네트워크 실패는 기존 캐시를 보존하며 10분 재시도 폭주를 만들지 않고 다음 정상 주기로 이연된다.
- 앱 재진입 시 기한 내 캐시를 표시하고 별도 날씨 API 요청을 만들지 않으며 앱과 위젯의 표시값과 아이콘이 일치한다.
- 신규 공급자 경로도 캐시 HIT 시 upstream을 호출하지 않고, 불완전하거나 오래된 응답은 판단 결과 생성에 사용하지 않는다.
- 활동예감용 14개 API의 승인 상태와 실제 응답 계약이 확인되고, 출처별 장애·누락·할당량 초과 폴백 테스트가 통과한다.
- 릴리즈 스크립트로 생성한 APK/AAB에서 Supabase 의존 기능이 실제 데이터로 동작한다.

## 17. 1차 제외 범위
- 민물낚시
- 조과 확률 또는 어획량 예측
- 해외 활동예감
- 활동 결과 위젯
- 서버 계정 기반 활동 이력 동기화
